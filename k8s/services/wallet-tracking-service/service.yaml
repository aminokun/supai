apiVersion: v1
kind: Service
metadata:
  name: wallet-tracking-service
  namespace: supai
  labels:
    app: wallet-tracking-service
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: wallet-tracking-service
