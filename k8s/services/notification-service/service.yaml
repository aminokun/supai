apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: supai
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
    name: http
  selector:
    app: notification-service
