apiVersion: v1
kind: Service
metadata:
  name: telegram-bot
  namespace: supai
  labels:
    app: telegram-bot
spec:
  type: ClusterIP
  ports:
  - port: 3006
    targetPort: 3006
    protocol: TCP
    name: http
  selector:
    app: telegram-bot
