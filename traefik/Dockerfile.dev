FROM node:20-alpine

WORKDIR /app

# Install dependencies (including devDependencies)
COPY package*.json ./
RUN npm install

# Copy source (will be overridden by volume mount)
COPY . .

EXPOSE 80

CMD ["npm", "run", "dev"]
