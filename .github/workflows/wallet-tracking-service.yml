name: Wallet Tracking Service CI

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'services/wallet-tracking/**'
      - '.github/workflows/wallet-tracking-service.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'services/wallet-tracking/**'

permissions:
  contents: read
  packages: write

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'services/wallet-tracking/package-lock.json'

      - name: Install dependencies
        working-directory: services/wallet-tracking
        run: npm ci

      - name: Run TypeScript check
        working-directory: services/wallet-tracking
        run: npx tsc --noEmit

      - name: Build
        working-directory: services/wallet-tracking
        run: npm run build --if-present

      - name: Run Snyk security scan
        working-directory: services/wallet-tracking
        run: |
          npm install -g snyk
          snyk auth ${{ secrets.SNYK_TOKEN }}
          snyk test --severity-threshold=high
          snyk monitor --project-name="supai-wallet-tracking"

      - name: Run tests
        working-directory: services/wallet-tracking
        run: npm test --if-present

      - name: Build Docker image
        run: |
          docker build \
            -t ghcr.io/${{ github.repository_owner }}/supai-wallet-tracking:${{ github.sha }} \
            -t ghcr.io/${{ github.repository_owner }}/supai-wallet-tracking:latest \
            services/wallet-tracking/

      - name: Push Docker image to GitHub Container Registry
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker push ghcr.io/${{ github.repository_owner }}/supai-wallet-tracking:${{ github.sha }}
          docker push ghcr.io/${{ github.repository_owner }}/supai-wallet-tracking:latest
