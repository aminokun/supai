name: Telegram Bot CI

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'frontend/telegram-bot/**'
      - '.github/workflows/telegram-bot.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'frontend/telegram-bot/**'

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'frontend/telegram-bot/package-lock.json'

      - name: Install dependencies
        working-directory: frontend/telegram-bot
        run: npm ci

      - name: Run TypeScript check
        working-directory: frontend/telegram-bot
        run: npx tsc --noEmit

      - name: Build
        working-directory: frontend/telegram-bot
        run: npm run build --if-present

      - name: Run Snyk security scan
        working-directory: frontend/telegram-bot
        run: |
          npm install -g snyk
          snyk auth ${{ secrets.SNYK_TOKEN }}
          snyk test --severity-threshold=high

      - name: Run tests
        working-directory: frontend/telegram-bot
        run: npm test --if-present

      - name: Build Docker image
        run: |
          docker build \
            -t ghcr.io/${{ github.repository_owner }}/supai-telegram-bot:${{ github.sha }} \
            -t ghcr.io/${{ github.repository_owner }}/supai-telegram-bot:latest \
            frontend/telegram-bot/

      - name: Push Docker image to GitHub Container Registry
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker push ghcr.io/${{ github.repository_owner }}/supai-telegram-bot:${{ github.sha }}
          docker push ghcr.io/${{ github.repository_owner }}/supai-telegram-bot:latest
